name: 🔬 과학 영상 자동 생성

# 매일 오전 9시 (Asia/Seoul) 자동 실행
on:
  schedule:
    - cron: '0 0 * * *'  # UTC 00:00 = KST 09:00
  workflow_dispatch:  # 수동 실행 가능

jobs:
  generate-science-video:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 Python 3.11 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 시스템 패키지 설치
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg imagemagick

      - name: 📚 Python 패키지 설치
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 출력 디렉토리 생성
        run: mkdir -p output/science logs

      - name: 🔍 파일 구조 확인 (디버깅)
        run: |
          echo "=== 현재 디렉토리 ==="
          pwd
          echo ""
          echo "=== deployment 폴더 내용 ==="
          ls -la deployment/
          echo ""
          echo "=== production_job_config.yaml 내용 (첫 10줄) ==="
          head -n 10 deployment/production_job_config.yaml

      - name: 🎬 과학 영상 생성
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-northeast-2
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
          TZ: Asia/Seoul
        run: |
          python runner.py \
            --job-config deployment/production_job_config.yaml \
            --job-name weekly-science-premium

      - name: 📤 영상 결과물 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: science-video-${{ github.run_number }}
          path: |
            output/science/**/*
            logs/*.log
          retention-days: 7

      - name: 📊 실행 결과 요약
        if: always()
        run: |
          echo "## 🎬 영상 생성 완료" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 📁 결과물: \`output/science/\`" >> $GITHUB_STEP_SUMMARY
          echo "- 📋 로그: \`logs/\`" >> $GITHUB_STEP_SUMMARY
          echo "- 📥 다운로드: Actions > Artifacts" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "output/science/"*.mp4 ]; then
            echo "✅ 영상 파일 생성 성공" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ 영상 파일 생성 실패" >> $GITHUB_STEP_SUMMARY
          fi
